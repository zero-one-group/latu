# Generated by dev/probe_functions.exs --write. Do not hand-edit.
#
# What every wrapped function was found to resolve with, measured against a live server:
# {name, arity, profile, over?}, where :override means the explicit arguments in the probe.
# Not a spec — a record. `test/latu/function_sweep_test.exs` asserts it still covers the
# surface exactly, so a function added by a Spark bump fails offline until the probe is
# re-run; re-running it and diffing is the check that the shapes have not moved.
%{
  resolved: [
    {:abs, 1, [:n], false},
    {:acos, 1, [:n], false},
    {:acosh, 1, [:n], false},
    {:add_months, 2, [:s], false},
    {:aes_decrypt, 2, [:s], false},
    {:aes_decrypt, 3, [:s], false},
    {:aes_decrypt, 4, [:s], false},
    {:aes_decrypt, 5, [:s], false},
    {:aes_encrypt, 2, [:s], false},
    {:aes_encrypt, 3, [:s], false},
    {:aes_encrypt, 4, [:s], false},
    {:aes_encrypt, 5, [:s], false},
    {:aes_encrypt, 6, [:s], false},
    {:any_value, 1, [:n], false},
    {:any_value, 2, [:n, :bool_lit], false},
    {:approx_count_distinct, 1, [:n], false},
    {:approx_count_distinct, 2, :override, false},
    {:approx_percentile, 2, [:n, :dbl_lit], false},
    {:approx_percentile, 3, :override, false},
    {:array, 1, [:n], false},
    {:array_agg, 1, [:n], false},
    {:array_append, 2, [:arr, :n], false},
    {:array_compact, 1, [:arr], false},
    {:array_contains, 2, [:arr, :n], false},
    {:array_distinct, 1, [:arr], false},
    {:array_except, 2, [:arr], false},
    {:array_insert, 3, :override, false},
    {:array_intersect, 2, [:arr], false},
    {:array_join, 2, [:arr, :s], false},
    {:array_join, 3, [:arr, :s], false},
    {:array_max, 1, [:arr], false},
    {:array_min, 1, [:arr], false},
    {:array_position, 2, [:arr, :n], false},
    {:array_prepend, 2, [:arr, :n], false},
    {:array_remove, 2, [:arr, :n], false},
    {:array_repeat, 2, [:n, :int_lit], false},
    {:array_size, 1, [:arr], false},
    {:array_union, 2, [:arr], false},
    {:arrays_overlap, 2, [:arr], false},
    {:arrays_zip, 1, [:arr], false},
    {:ascii, 1, [:n], false},
    {:asin, 1, [:n], false},
    {:asinh, 1, [:n], false},
    {:assert_true, 1, [:b], false},
    {:assert_true, 2, [:b], false},
    {:atan, 1, [:n], false},
    {:atan2, 2, [:n], false},
    {:atanh, 1, [:n], false},
    {:avg, 1, [:n], false},
    {:base64, 1, [:s], false},
    {:bin, 1, [:n], false},
    {:bit_and, 1, [:n], false},
    {:bit_count, 1, [:n], false},
    {:bit_get, 2, [:n], false},
    {:bit_length, 1, [:n], false},
    {:bit_or, 1, [:n], false},
    {:bit_xor, 1, [:n], false},
    {:bitmap_and_agg, 1, [:bin], false},
    {:bitmap_bit_position, 1, [:n], false},
    {:bitmap_bucket_number, 1, [:n], false},
    {:bitmap_construct_agg, 1, [:n], false},
    {:bitmap_count, 1, [:bin], false},
    {:bitmap_or_agg, 1, [:bin], false},
    {:bitwise_not, 1, [:n], false},
    {:bool_and, 1, [:s], false},
    {:bool_or, 1, [:s], false},
    {:bround, 1, [:n], false},
    {:bround, 2, [:n, :int_lit], false},
    {:btrim, 1, [:n], false},
    {:btrim, 2, [:n], false},
    {:cardinality, 1, [:arr], false},
    {:cbrt, 1, [:n], false},
    {:ceil, 1, [:n], false},
    {:ceil, 2, [:n, :int_lit], false},
    {:ceiling, 1, [:n], false},
    {:ceiling, 2, [:n, :int_lit], false},
    {:char, 1, [:n], false},
    {:char_length, 1, [:n], false},
    {:character_length, 1, [:n], false},
    {:chr, 1, [:n], false},
    {:coalesce, 1, [:n], false},
    {:collate, 2, :override, false},
    {:collation, 1, [:s], false},
    {:collect_list, 1, [:n], false},
    {:collect_set, 1, [:n], false},
    {:concat, 1, [:n], false},
    {:concat_ws, 2, [:n], false},
    {:conv, 3, [:n], false},
    {:corr, 2, [:n], false},
    {:cos, 1, [:n], false},
    {:cosh, 1, [:n], false},
    {:cot, 1, [:n], false},
    {:count, 1, [:n], false},
    {:count_distinct, 1, [:n], false},
    {:count_distinct, 2, [:n], false},
    {:count_if, 1, [:s], false},
    {:covar_pop, 2, [:n], false},
    {:covar_samp, 2, [:n], false},
    {:crc32, 1, [:s], false},
    {:create_map, 1, [:n], false},
    {:csc, 1, [:n], false},
    {:cume_dist, 0, [:n], true},
    {:curdate, 0, [:n], false},
    {:current_catalog, 0, [:n], false},
    {:current_database, 0, [:n], false},
    {:current_date, 0, [:n], false},
    {:current_path, 0, [:n], false},
    {:current_schema, 0, [:n], false},
    {:current_time, 0, [:n], false},
    {:current_time, 1, [:int_lit], false},
    {:current_timestamp, 0, [:n], false},
    {:current_timezone, 0, [:n], false},
    {:current_user, 0, [:n], false},
    {:date_add, 2, [:s], false},
    {:date_diff, 2, [:s], false},
    {:date_format, 2, [:s], false},
    {:date_from_unix_date, 1, [:n], false},
    {:date_part, 2, [:field_lit, :ts], false},
    {:date_sub, 2, [:s], false},
    {:date_trunc, 2, [:s], false},
    {:dateadd, 2, [:s], false},
    {:datediff, 2, [:s], false},
    {:datepart, 2, [:field_lit, :ts], false},
    {:day, 1, [:s], false},
    {:dayname, 1, [:s], false},
    {:dayofmonth, 1, [:s], false},
    {:dayofweek, 1, [:s], false},
    {:dayofyear, 1, [:s], false},
    {:decode, 2, [:s], false},
    {:degrees, 1, [:n], false},
    {:dense_rank, 0, [:n], true},
    {:e, 0, [:n], false},
    {:element_at, 2, [:arr, :int_lit], false},
    {:elt, 1, [:n], false},
    {:encode, 2, [:n], false},
    {:equal_null, 2, [:n], false},
    {:every, 1, [:s], false},
    {:exp, 1, [:n], false},
    {:explode, 1, [:arr], false},
    {:explode_outer, 1, [:arr], false},
    {:expm1, 1, [:n], false},
    {:extract, 2, [:field_lit, :ts], false},
    {:factorial, 1, [:n], false},
    {:find_in_set, 2, [:n], false},
    {:first, 1, [:n], false},
    {:first, 2, [:n, :bool_lit], false},
    {:first_value, 1, [:n], false},
    {:first_value, 2, [:n, :bool_lit], false},
    {:flatten, 1, [:aarr], false},
    {:floor, 1, [:n], false},
    {:floor, 2, [:n, :int_lit], false},
    {:format_number, 2, [:n, :s], false},
    {:format_string, 2, [:n], false},
    {:from_unixtime, 1, [:n], false},
    {:from_unixtime, 2, [:n], false},
    {:from_utc_timestamp, 2, [:s], false},
    {:get, 2, [:arr, :n], false},
    {:get_json_object, 2, [:s], false},
    {:getbit, 2, [:n], false},
    {:greatest, 1, [:n], false},
    {:hash, 1, [:n], false},
    {:hex, 1, [:n], false},
    {:histogram_numeric, 2, [:n, :int_lit], false},
    {:hll_sketch_agg, 1, [:n], false},
    {:hll_sketch_agg, 2, [:n, :int_lit], false},
    {:hll_sketch_estimate, 1, [:bin], false},
    {:hour, 1, [:s], false},
    {:hypot, 2, [:n], false},
    {:ifnull, 2, [:n], false},
    {:initcap, 1, [:n], false},
    {:input_file_block_length, 0, [:n], false},
    {:input_file_block_start, 0, [:n], false},
    {:input_file_name, 0, [:n], false},
    {:instr, 2, [:n], false},
    {:is_valid_utf8, 1, [:n], false},
    {:is_valid_variant, 1, [:var], false},
    {:is_variant_null, 1, [:var], false},
    {:json_array_length, 1, [:s], false},
    {:json_object_keys, 1, [:s], false},
    {:json_tuple, 2, [:s], false},
    {:kll_sketch_get_n_bigint, 1, [:s], false},
    {:kll_sketch_get_n_double, 1, [:s], false},
    {:kll_sketch_get_n_float, 1, [:s], false},
    {:kll_sketch_get_quantile_bigint, 2, [:s, :str_lit], false},
    {:kll_sketch_get_quantile_double, 2, [:s, :str_lit], false},
    {:kll_sketch_get_quantile_float, 2, [:s, :str_lit], false},
    {:kll_sketch_get_rank_bigint, 2, [:s, :str_lit], false},
    {:kll_sketch_get_rank_double, 2, [:s, :str_lit], false},
    {:kll_sketch_get_rank_float, 2, [:s, :str_lit], false},
    {:kll_sketch_merge_bigint, 2, [:s], false},
    {:kll_sketch_merge_double, 2, [:s], false},
    {:kll_sketch_merge_float, 2, [:s], false},
    {:kll_sketch_to_string_bigint, 1, [:s], false},
    {:kll_sketch_to_string_double, 1, [:s], false},
    {:kll_sketch_to_string_float, 1, [:s], false},
    {:kurtosis, 1, [:n], false},
    {:last, 1, [:n], false},
    {:last, 2, [:n, :bool_lit], false},
    {:last_day, 1, [:s], false},
    {:last_value, 1, [:n], false},
    {:last_value, 2, [:n, :bool_lit], false},
    {:lcase, 1, [:n], false},
    {:least, 1, [:n], false},
    {:left, 2, [:n], false},
    {:length, 1, [:n], false},
    {:levenshtein, 2, [:n], false},
    {:levenshtein, 3, [:n], false},
    {:listagg, 1, [:n], false},
    {:listagg, 2, [:n, :int_lit], false},
    {:ln, 1, [:n], false},
    {:localtimestamp, 0, [:n], false},
    {:locate, 2, [:n], false},
    {:locate, 3, [:n], false},
    {:log10, 1, [:n], false},
    {:log1p, 1, [:n], false},
    {:log2, 1, [:n], false},
    {:lower, 1, [:n], false},
    {:lpad, 3, [:n], false},
    {:make_date, 3, [:n], false},
    {:make_dt_interval, 0, [:n], false},
    {:make_dt_interval, 1, [:n], false},
    {:make_dt_interval, 2, [:n], false},
    {:make_dt_interval, 3, [:n], false},
    {:make_dt_interval, 4, [:n], false},
    {:make_interval, 0, [:n], false},
    {:make_interval, 1, [:n], false},
    {:make_interval, 2, [:n], false},
    {:make_interval, 3, [:n], false},
    {:make_interval, 4, [:n], false},
    {:make_interval, 5, [:n], false},
    {:make_interval, 6, [:n], false},
    {:make_interval, 7, [:n], false},
    {:make_timestamp_ltz, 6, [:n], false},
    {:make_timestamp_ltz, 7, [:n], false},
    {:make_valid_utf8, 1, [:n], false},
    {:make_ym_interval, 0, [:n], false},
    {:make_ym_interval, 1, [:n], false},
    {:make_ym_interval, 2, [:n], false},
    {:map_concat, 1, [:m], false},
    {:map_contains_key, 2, [:m, :s], false},
    {:map_entries, 1, [:m], false},
    {:map_from_arrays, 2, [:arr], false},
    {:map_from_entries, 1, [:starr], false},
    {:map_keys, 1, [:m], false},
    {:map_values, 1, [:m], false},
    {:mask, 1, [:s], false},
    {:mask, 2, [:s, :str_lit], false},
    {:mask, 3, [:s, :str_lit], false},
    {:mask, 4, [:s, :str_lit], false},
    {:mask, 5, [:s, :str_lit], false},
    {:max, 1, [:n], false},
    {:max_by, 2, [:n], false},
    {:max_by, 3, [:n, :int_lit], false},
    {:md5, 1, [:s], false},
    {:median, 1, [:n], false},
    {:min, 1, [:n], false},
    {:min_by, 2, [:n], false},
    {:min_by, 3, [:n, :int_lit], false},
    {:minute, 1, [:s], false},
    {:mode, 1, [:n], false},
    {:mode, 2, [:n, :bool_lit], false},
    {:monotonically_increasing_id, 0, [:n], false},
    {:month, 1, [:s], false},
    {:monthname, 1, [:s], false},
    {:months_between, 2, [:s], false},
    {:months_between, 3, [:s], false},
    {:named_struct, 1, [:str_lit], false},
    {:nanvl, 2, [:n], false},
    {:negative, 1, [:n], false},
    {:next_day, 2, [:s], false},
    {:now, 0, [:n], false},
    {:nth_value, 2, [:n, :int_lit], true},
    {:nth_value, 3, :override, true},
    {:ntile, 1, [:int_lit], true},
    {:nullif, 2, [:n], false},
    {:nullifzero, 1, [:n], false},
    {:nvl, 2, [:n], false},
    {:nvl2, 3, [:n], false},
    {:octet_length, 1, [:n], false},
    {:overlay, 3, [:n], false},
    {:overlay, 4, [:n], false},
    {:parse_json, 1, [:s], false},
    {:parse_url, 2, [:s], false},
    {:parse_url, 3, [:s], false},
    {:percent_rank, 0, [:n], true},
    {:percentile, 2, [:n, :dbl_lit], false},
    {:percentile, 3, :override, false},
    {:percentile_approx, 2, [:n, :dbl_lit], false},
    {:percentile_approx, 3, :override, false},
    {:pi, 0, [:n], false},
    {:pmod, 2, [:n], false},
    {:position, 2, [:n], false},
    {:position, 3, [:n], false},
    {:positive, 1, [:n], false},
    {:printf, 2, [:n], false},
    {:product, 1, [:n], false},
    {:quarter, 1, [:s], false},
    {:quote, 1, [:n], false},
    {:radians, 1, [:n], false},
    {:raise_error, 1, [:n], false},
    {:rank, 0, [:n], true},
    {:regexp, 2, [:n], false},
    {:regexp_count, 2, [:n], false},
    {:regexp_extract, 3, [:n], false},
    {:regexp_extract_all, 2, [:n], false},
    {:regexp_extract_all, 3, [:n], false},
    {:regexp_instr, 2, [:n], false},
    {:regexp_instr, 3, [:n], false},
    {:regexp_like, 2, [:n], false},
    {:regexp_replace, 3, [:n], false},
    {:regexp_substr, 2, [:n], false},
    {:regr_avgx, 2, [:n], false},
    {:regr_avgy, 2, [:n], false},
    {:regr_count, 2, [:n], false},
    {:regr_intercept, 2, [:n], false},
    {:regr_r2, 2, [:n], false},
    {:regr_slope, 2, [:n], false},
    {:regr_sxx, 2, [:n], false},
    {:regr_sxy, 2, [:n], false},
    {:regr_syy, 2, [:n], false},
    {:repeat, 2, [:n], false},
    {:replace, 2, [:n], false},
    {:replace, 3, [:n], false},
    {:reverse, 1, [:n], false},
    {:right, 2, [:n], false},
    {:rint, 1, [:n], false},
    {:round, 1, [:n], false},
    {:round, 2, [:n, :int_lit], false},
    {:row_number, 0, [:n], true},
    {:rpad, 3, [:n], false},
    {:schema_of_variant, 1, [:var], false},
    {:schema_of_variant_agg, 1, [:var], false},
    {:sec, 1, [:n], false},
    {:second, 1, [:s], false},
    {:sentences, 1, [:n], false},
    {:sentences, 2, [:n], false},
    {:sentences, 3, [:n], false},
    {:sequence, 2, [:n], false},
    {:sequence, 3, [:n], false},
    {:session_user, 0, [:n], false},
    {:session_window, 2, [:s], false},
    {:sha, 1, [:s], false},
    {:sha1, 1, [:s], false},
    {:sha2, 2, [:s], false},
    {:shiftleft, 2, [:n], false},
    {:shiftright, 2, [:n], false},
    {:shiftrightunsigned, 2, [:n], false},
    {:sign, 1, [:n], false},
    {:signum, 1, [:n], false},
    {:sin, 1, [:n], false},
    {:sinh, 1, [:n], false},
    {:size, 1, [:arr], false},
    {:skewness, 1, [:n], false},
    {:slice, 3, [:arr, :n], false},
    {:some, 1, [:s], false},
    {:sort_array, 1, [:arr], false},
    {:sort_array, 2, [:arr, :bool_lit], false},
    {:soundex, 1, [:s], false},
    {:spark_partition_id, 0, [:n], false},
    {:split, 2, [:n], false},
    {:split, 3, [:n], false},
    {:split_part, 3, [:n], false},
    {:sqrt, 1, [:n], false},
    {:st_geogfromwkb, 1, [:s], false},
    {:st_geomfromwkb, 1, [:s], false},
    {:st_geomfromwkb, 2, [:s], false},
    {:stack, 1, [:int_lit], false},
    {:std, 1, [:n], false},
    {:stddev, 1, [:n], false},
    {:stddev_pop, 1, [:n], false},
    {:stddev_samp, 1, [:n], false},
    {:str_to_map, 1, [:s], false},
    {:str_to_map, 2, [:s], false},
    {:str_to_map, 3, [:s], false},
    {:string_agg, 1, [:n], false},
    {:string_agg, 2, [:n, :int_lit], false},
    {:struct, 1, [:n], false},
    {:substr, 2, [:n], false},
    {:substr, 3, [:n], false},
    {:substring, 3, [:n], false},
    {:substring_index, 3, [:n], false},
    {:sum, 1, [:n], false},
    {:sum_distinct, 1, [:n], false},
    {:tan, 1, [:n], false},
    {:tanh, 1, [:n], false},
    {:theta_difference, 2, [:bin], false},
    {:theta_intersection, 2, [:bin], false},
    {:theta_intersection_agg, 1, [:bin], false},
    {:theta_sketch_agg, 1, [:n], false},
    {:theta_sketch_agg, 2, [:n, :int_lit], false},
    {:theta_sketch_estimate, 1, [:bin], false},
    {:time_bucket, 2, :override, false},
    {:time_bucket, 3, :override, false},
    {:timestamp_add, 3, [:str_lit], false},
    {:timestamp_diff, 3, [:str_lit], false},
    {:timestamp_micros, 1, [:n], false},
    {:timestamp_millis, 1, [:n], false},
    {:timestamp_seconds, 1, [:n], false},
    {:to_binary, 1, [:n], false},
    {:to_binary, 2, :override, false},
    {:to_char, 2, [:ts], false},
    {:to_date, 1, [:n], false},
    {:to_date, 2, [:n], false},
    {:to_number, 2, :override, false},
    {:to_timestamp, 1, [:n], false},
    {:to_timestamp, 2, [:s], false},
    {:to_timestamp_ltz, 1, [:n], false},
    {:to_timestamp_ltz, 2, [:s], false},
    {:to_timestamp_ntz, 1, [:n], false},
    {:to_timestamp_ntz, 2, [:n], false},
    {:to_unix_timestamp, 1, [:s], false},
    {:to_unix_timestamp, 2, [:s], false},
    {:to_utc_timestamp, 2, [:s], false},
    {:to_varchar, 2, [:ts], false},
    {:to_variant_object, 1, [:arr], false},
    {:translate, 3, [:n], false},
    {:trunc, 2, [:s], false},
    {:try_add, 2, [:n], false},
    {:try_aes_decrypt, 2, [:s], false},
    {:try_aes_decrypt, 3, [:s], false},
    {:try_aes_decrypt, 4, [:s], false},
    {:try_aes_decrypt, 5, [:s], false},
    {:try_avg, 1, [:n], false},
    {:try_divide, 2, [:n], false},
    {:try_element_at, 2, [:arr, :int_lit], false},
    {:try_make_interval, 0, [:n], false},
    {:try_make_interval, 1, [:n], false},
    {:try_make_interval, 2, [:n], false},
    {:try_make_interval, 3, [:n], false},
    {:try_make_interval, 4, [:n], false},
    {:try_make_interval, 5, [:n], false},
    {:try_make_interval, 6, [:n], false},
    {:try_make_interval, 7, [:n], false},
    {:try_make_timestamp_ltz, 6, [:n], false},
    {:try_make_timestamp_ltz, 7, [:n], false},
    {:try_mod, 2, [:n], false},
    {:try_multiply, 2, [:n], false},
    {:try_parse_json, 1, [:s], false},
    {:try_parse_url, 2, [:s], false},
    {:try_parse_url, 3, [:s], false},
    {:try_subtract, 2, [:n], false},
    {:try_sum, 1, [:n], false},
    {:try_to_binary, 1, [:n], false},
    {:try_to_binary, 2, [:n, :int_lit], false},
    {:try_to_date, 1, [:n], false},
    {:try_to_date, 2, [:n], false},
    {:try_to_number, 2, :override, false},
    {:try_to_timestamp, 1, [:n], false},
    {:try_to_timestamp, 2, [:s], false},
    {:try_url_decode, 1, [:n], false},
    {:try_validate_utf8, 1, [:n], false},
    {:try_variant_get, 3, :override, false},
    {:tuple_difference_double, 2, [:bin], false},
    {:tuple_difference_integer, 2, [:bin], false},
    {:tuple_difference_theta_double, 2, [:bin], false},
    {:tuple_difference_theta_integer, 2, [:bin], false},
    {:tuple_sketch_estimate_double, 1, [:bin], false},
    {:tuple_sketch_estimate_integer, 1, [:bin], false},
    {:tuple_sketch_theta_double, 1, [:bin], false},
    {:tuple_sketch_theta_integer, 1, [:bin], false},
    {:typeof, 1, [:n], false},
    {:ucase, 1, [:n], false},
    {:unbase64, 1, [:n], false},
    {:unhex, 1, [:n], false},
    {:unix_date, 1, [:dt], false},
    {:unix_micros, 1, [:ts], false},
    {:unix_millis, 1, [:ts], false},
    {:unix_seconds, 1, [:ts], false},
    {:upper, 1, [:n], false},
    {:url_decode, 1, [:n], false},
    {:url_encode, 1, [:n], false},
    {:user, 0, [:n], false},
    {:validate_utf8, 1, [:n], false},
    {:var_pop, 1, [:n], false},
    {:var_samp, 1, [:n], false},
    {:variance, 1, [:n], false},
    {:variant_get, 3, :override, false},
    {:version, 0, [:n], false},
    {:weekday, 1, [:s], false},
    {:weekofyear, 1, [:s], false},
    {:width_bucket, 4, [:n], false},
    {:window_time, 1, :override, false},
    {:xpath, 2, [:s, :str_lit], false},
    {:xpath_boolean, 2, [:s, :str_lit], false},
    {:xpath_double, 2, [:s, :str_lit], false},
    {:xpath_float, 2, [:s, :str_lit], false},
    {:xpath_int, 2, [:s, :str_lit], false},
    {:xpath_long, 2, [:s, :str_lit], false},
    {:xpath_number, 2, [:s, :str_lit], false},
    {:xpath_short, 2, [:s, :str_lit], false},
    {:xpath_string, 2, [:s, :str_lit], false},
    {:xxhash64, 1, [:n], false},
    {:year, 1, [:s], false},
    {:zeroifnull, 1, [:n], false},
    {:aggregate, 3, :override, false},
    {:aggregate, 4, :override, false},
    {:array_sort, 1, [:arr], false},
    {:array_sort, 2, :override, false},
    {:call_function, 2, :override, false},
    {:convert_timezone, 2, [:s], false},
    {:convert_timezone, 3, [:s], false},
    {:count_min_sketch, 3, :override, false},
    {:count_min_sketch, 4, :override, false},
    {:exists, 2, :override, false},
    {:filter, 2, :override, false},
    {:forall, 2, :override, false},
    {:from_csv, 2, :override, false},
    {:from_csv, 3, :override, false},
    {:from_json, 2, :override, false},
    {:from_json, 3, :override, false},
    {:from_xml, 2, :override, false},
    {:from_xml, 3, :override, false},
    {:lag, 1, [:n], true},
    {:lag, 2, [:n, :int_lit], true},
    {:lag, 3, [:n, :int_lit], true},
    {:lead, 1, [:n], true},
    {:lead, 2, [:n, :int_lit], true},
    {:lead, 3, [:n, :int_lit], true},
    {:listagg_distinct, 1, [:n], false},
    {:listagg_distinct, 2, [:n, :int_lit], false},
    {:log, 1, [:n], false},
    {:log, 2, [:n], false},
    {:ltrim, 1, [:n], false},
    {:ltrim, 2, [:n], false},
    {:make_timestamp, 6, [:n], false},
    {:make_timestamp, 7, [:n], false},
    {:make_timestamp_ntz, 6, [:n], false},
    {:map_filter, 2, :override, false},
    {:map_zip_with, 3, :override, false},
    {:mean, 1, [:n], false},
    {:otherwise, 2, :override, false},
    {:rand, 0, [:n], false},
    {:rand, 1, [:int_lit], false},
    {:randn, 0, [:n], false},
    {:randn, 1, [:int_lit], false},
    {:randstr, 1, [:str_lit], false},
    {:randstr, 2, [:int_lit], false},
    {:reduce, 3, :override, false},
    {:reduce, 4, :override, false},
    {:rtrim, 1, [:n], false},
    {:rtrim, 2, [:n], false},
    {:schema_of_csv, 1, [:str_lit], false},
    {:schema_of_csv, 2, :override, false},
    {:schema_of_json, 1, [:str_lit], false},
    {:schema_of_json, 2, :override, false},
    {:schema_of_xml, 1, [:str_lit], false},
    {:schema_of_xml, 2, :override, false},
    {:shuffle, 1, [:arr], false},
    {:shuffle, 2, :override, false},
    {:string_agg_distinct, 1, [:n], false},
    {:string_agg_distinct, 2, [:n, :int_lit], false},
    {:to_csv, 1, [:st], false},
    {:to_csv, 2, :override, false},
    {:to_json, 1, [:arr], false},
    {:to_json, 2, :override, false},
    {:to_xml, 1, [:st], false},
    {:to_xml, 2, :override, false},
    {:transform, 2, :override, false},
    {:transform_keys, 2, :override, false},
    {:transform_values, 2, :override, false},
    {:trim, 1, [:n], false},
    {:trim, 2, [:n], false},
    {:try_make_timestamp, 6, [:n], false},
    {:try_make_timestamp, 7, [:n], false},
    {:try_make_timestamp_ntz, 6, [:n], false},
    {:uniform, 2, [:int_lit], false},
    {:uniform, 3, [:int_lit], false},
    {:unix_timestamp, 0, [:n], false},
    {:unix_timestamp, 1, [:s], false},
    {:unix_timestamp, 2, [:s], false},
    {:uuid, 0, [:n], false},
    {:uuid, 1, [:int_lit], false},
    {:when_, 2, [:b], false},
    {:when_, 3, :override, false},
    {:window, 2, [:s, :int_lit], false},
    {:window, 3, [:s, :int_lit], false},
    {:window, 4, :override, false},
    {:zip_with, 3, :override, false}
  ],
  excluded: [
    {:time_from_seconds,
     "Spark 4.2 refuses the TIME type outright: UNSUPPORTED_TIME_TYPE. Nothing to do until a Spark that ships it"},
    {:java_method,
     "calls a JVM method by name, so the class must be a foldable string naming something on the server's classpath"},
    {:time_from_millis,
     "Spark 4.2 refuses the TIME type outright: UNSUPPORTED_TIME_TYPE. Nothing to do until a Spark that ships it"},
    {:posexplode_outer,
     "a generator that expands to more than one column, so a single aliased projection cannot hold it — explode and explode_outer yield one column and are swept normally"},
    {{:try_make_timestamp, 3}, "the short forms take a date and a TIME, which Spark 4.2 refuses"},
    {{:try_make_timestamp_ntz, 2},
     "the short forms take a date and a TIME, which Spark 4.2 refuses"},
    {:st_asbinary, "needs a GEOGRAPHY or GEOMETRY column, which Latu has no way to build"},
    {:theta_union,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:reflect,
     "calls a JVM method by name, so the class must be a foldable string naming something on the server's classpath"},
    {:hll_union,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:tuple_union_double,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:tuple_sketch_agg_integer,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:to_time,
     "Spark 4.2 refuses the TIME type outright: UNSUPPORTED_TIME_TYPE. Nothing to do until a Spark that ships it"},
    {:grouping,
     "only valid inside a cube, rollup or grouping-sets aggregate, and the sweep builds a projection"},
    {:time_from_micros,
     "Spark 4.2 refuses the TIME type outright: UNSUPPORTED_TIME_TYPE. Nothing to do until a Spark that ships it"},
    {:tuple_intersection_theta_double,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:kll_merge_agg_bigint,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:unwrap_udt,
     "needs a user-defined-type column; Latu ships no UDT and design 12 rules them out"},
    {:st_srid, "needs a GEOGRAPHY or GEOMETRY column, which Latu has no way to build"},
    {:grouping_id,
     "only valid inside a cube, rollup or grouping-sets aggregate, and the sweep builds a projection"},
    {:tuple_sketch_agg_double,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:time_to_millis,
     "Spark 4.2 refuses the TIME type outright: UNSUPPORTED_TIME_TYPE. Nothing to do until a Spark that ships it"},
    {:inline_outer,
     "a generator that expands to more than one column, so a single aliased projection cannot hold it — explode and explode_outer yield one column and are swept normally"},
    {:time_diff,
     "Spark 4.2 refuses the TIME type outright: UNSUPPORTED_TIME_TYPE. Nothing to do until a Spark that ships it"},
    {:tuple_union_theta_double,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:kll_sketch_agg_float,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:tuple_union_agg_integer,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:kll_merge_agg_double,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:kll_sketch_agg_double,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:tuple_intersection_agg_integer,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:tuple_sketch_summary_integer,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {{:make_timestamp, 2}, "the short forms take a date and a TIME, which Spark 4.2 refuses"},
    {:theta_union_agg,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:tuple_intersection_integer,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:tuple_union_integer,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {{:make_timestamp_ntz, 2}, "the short forms take a date and a TIME, which Spark 4.2 refuses"},
    {:time_trunc,
     "Spark 4.2 refuses the TIME type outright: UNSUPPORTED_TIME_TYPE. Nothing to do until a Spark that ships it"},
    {:make_time,
     "Spark 4.2 refuses the TIME type outright: UNSUPPORTED_TIME_TYPE. Nothing to do until a Spark that ships it"},
    {:inline,
     "a generator that expands to more than one column, so a single aliased projection cannot hold it — explode and explode_outer yield one column and are swept normally"},
    {:tuple_union_agg_double,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:time_to_seconds,
     "Spark 4.2 refuses the TIME type outright: UNSUPPORTED_TIME_TYPE. Nothing to do until a Spark that ships it"},
    {:tuple_intersection_double,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:tuple_union_theta_integer,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:time_to_micros,
     "Spark 4.2 refuses the TIME type outright: UNSUPPORTED_TIME_TYPE. Nothing to do until a Spark that ships it"},
    {:kll_merge_agg_float,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:try_reflect,
     "calls a JVM method by name, so the class must be a foldable string naming something on the server's classpath"},
    {:kll_sketch_agg_bigint,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:hll_union_agg,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:tuple_intersection_agg_double,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {:tuple_intersection_theta_integer,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {{:make_timestamp, 3}, "the short forms take a date and a TIME, which Spark 4.2 refuses"},
    {:st_setsrid, "needs a GEOGRAPHY or GEOMETRY column, which Latu has no way to build"},
    {:try_to_time,
     "Spark 4.2 refuses the TIME type outright: UNSUPPORTED_TIME_TYPE. Nothing to do until a Spark that ships it"},
    {:posexplode,
     "a generator that expands to more than one column, so a single aliased projection cannot hold it — explode and explode_outer yield one column and are swept normally"},
    {:tuple_sketch_summary_double,
     "takes a sketch buffer produced by the matching *_agg function; one projection cannot make one, and nesting the aggregates is not a plan Spark accepts"},
    {{:try_make_timestamp, 2}, "the short forms take a date and a TIME, which Spark 4.2 refuses"}
  ]
}
