# generated by pyspark_oracle.py
#   spark.readStream.format("parquet").schema("id BIGINT").option("maxFilesPerTrigger", 1).load("/tmp/latu/src")

root {
  common {
    plan_id: 0
  }
  read {
    data_source {
      format: "parquet"
      schema: "id BIGINT"
      options {
        key: "maxFilesPerTrigger"
        value: "1"
      }
      paths: "/tmp/latu/src"
    }
    is_streaming: true
  }
}
