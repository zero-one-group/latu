# generated by pyspark_oracle.py
#   catalog_plan(cat.ListTables(db_name="default", pattern="latu_*"))

root {
  common {
    plan_id: 0
  }
  catalog {
    list_tables {
      db_name: "default"
      pattern: "latu_*"
    }
  }
}
