# generated by pyspark_oracle.py
#   sql_cmd("SELECT 1 AS one")

command {
  sql_command {
    input {
      common {
        plan_id: 0
      }
      sql {
        query: "SELECT 1 AS one"
      }
    }
  }
}
