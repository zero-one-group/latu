# generated by pyspark_oracle.py
#   spark.read.options(mergeSchema=True).table("people")

root {
  common {
    plan_id: 0
  }
  read {
    named_table {
      unparsed_identifier: "people"
      options {
        key: "mergeSchema"
        value: "true"
      }
    }
  }
}
