# generated by pyspark_oracle.py
#   command_plan(cat.RemoveRemoteCachedRelation(cat.CachedRemoteRelation("abc", None)).command(spark.client))

command {
  remove_cached_remote_relation_command {
    relation {
      relation_id: "abc"
    }
  }
}
