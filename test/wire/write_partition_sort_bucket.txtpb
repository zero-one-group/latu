# generated by pyspark_oracle.py
#   save(spark.range(10).write.format("parquet").partitionBy("id").bucketBy(4, "id").sortBy("id"), "/tmp/latu/buckets")

command {
  write_operation {
    input {
      common {
        plan_id: 0
      }
      range {
        start: 0
        end: 10
        step: 1
      }
    }
    source: "parquet"
    path: "/tmp/latu/buckets"
    sort_column_names: "id"
    partitioning_columns: "id"
    bucket_by {
      bucket_column_names: "id"
      num_buckets: 4
    }
  }
}
