# generated by pyspark_oracle.py
#   capture(lambda: spark.streams.awaitAnyTermination(5))

command {
  streaming_query_manager_command {
    await_any_termination {
      timeout_ms: 5000
    }
  }
}
