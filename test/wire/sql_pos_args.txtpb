# generated by pyspark_oracle.py
#   sql_cmd("SELECT ? AS n, ? AS s", [42, "x"])

command {
  sql_command {
    input {
      common {
        plan_id: 0
      }
      sql {
        query: "SELECT ? AS n, ? AS s"
        pos_arguments {
          literal {
            integer: 42
          }
        }
        pos_arguments {
          literal {
            string: "x"
          }
        }
      }
    }
  }
}
