# generated by pyspark_oracle.py
#   capture(lambda: query().lastProgress)

command {
  streaming_query_command {
    query_id {
      id: "q-1"
      run_id: "r-1"
    }
    last_progress: true
  }
}
