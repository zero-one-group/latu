# generated by pyspark_oracle.py
#   spark.read.format("csv").schema("id INT, name STRING").options(header=True, sep=";").load("/fixtures/people.csv")

root {
  common {
    plan_id: 0
  }
  read {
    data_source {
      format: "csv"
      schema: "id INT, name STRING"
      options {
        key: "header"
        value: "true"
      }
      options {
        key: "sep"
        value: ";"
      }
      paths: "/fixtures/people.csv"
    }
  }
}
