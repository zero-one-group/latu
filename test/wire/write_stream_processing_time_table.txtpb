# generated by pyspark_oracle.py
#   capture(lambda: spark.readStream.format("rate").load().writeStream.trigger(processingTime="10 seconds").clusterBy("value").toTable("sink_tbl"))

command {
  write_stream_operation_start {
    input {
      common {
        plan_id: 0
      }
      read {
        data_source {
          format: "rate"
          schema: ""
        }
        is_streaming: true
      }
    }
    processing_time_interval: "10 seconds"
    table_name: "sink_tbl"
    clustering_column_names: "value"
  }
}
